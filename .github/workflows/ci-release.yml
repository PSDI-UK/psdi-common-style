# Pipeline to run on release branch only

name: ci-release
run-name: ci-release
on:
  workflow_dispatch:
  push:
    branches: ["release"]

jobs:
  tag-release:
    uses: ./.github/workflows/tag-release.yml
